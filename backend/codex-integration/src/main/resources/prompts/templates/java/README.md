# Java Language-Specific Prompt Templates

Place Java-specific prompt templates here, e.g.:
- `java-vuln-analysis.yaml`
- `java-false-positive-filter.yaml`

These will be loaded as overrides for the default templates when the scanned language is Java.
