gitlab:
  base-url: http://localhost:8089
  private-token: glpat-test-mock-token
  connect-timeout-seconds: 5
  read-timeout-seconds: 10
  mode: mock
  webhook-secret: test-webhook-secret-64charhexstring

server:
  port: 0

spring:
  application:
    name: gitlab-integration-test

logging:
  level:
    com.codesec.gitlab: DEBUG
    okhttp3: WARN
