server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codesec
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

codesec:
  crypto:
    provider: aes  # aes | kms
    master-key: "MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI="  # 32-byte base64 key (dev only, used when provider=aes)
    kms:
      region: ${KMS_REGION:cn-hangzhou}
      key-id: ${KMS_KEY_ID:}
  jwt:
    secret: "Y29kZVNlY1BsYXRmb3JtU2VjcmV0S2V5Rm9ySFMyNTY="  # HS256 key (dev only)
    expiration: 86400000  # 24h in ms
  engine:
    sandbox: false

  cpg:
    enabled: true
    uri: ${NEO4J_URI:bolt://localhost:7687}
    username: ${NEO4J_USERNAME:neo4j}
    password: ${NEO4J_PASSWORD:admin123}

codex:
  enabled: true
  max-concurrency: 20
  per-request-timeout-seconds: 120
  code-model:
    model: ${CODEX_CODE_MODEL:gpt-4o}
    max-tokens: 4096
    temperature: 0.1
    timeout-seconds: 120
  llm-model:
    model: ${CODEX_LLM_MODEL:gpt-4o}
    max-tokens: 8192
    temperature: 0.3
    timeout-seconds: 120
  capabilities:
    vuln-analysis: true
    false-positive-filter: true
    poc-generation: true
    patch-generation: true
    logic-vuln-mining: true

logging:
  level:
    com.codesec: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
